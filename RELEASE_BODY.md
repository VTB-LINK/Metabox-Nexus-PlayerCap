### 3.0.0-beta.11

这是 3.0.0-beta.11 预发布版本，主要修复酷狗音乐的封面获取问题并优化性能。

#### Bug 修复

- **修复酷狗伴奏曲目无封面问题**：重构封面获取逻辑，伴奏、纯音乐等无歌词曲目现在也能正确获取和推送封面
- **修复酷狗未安装时持续重试问题**：当检测到酷狗未安装时，立即停止重试循环，避免产生大量无效日志

#### 性能优化

- **优化酷狗封面获取速度**：将封面 API 降级超时从 3 秒降低到 800 毫秒，减少等待时间，提升响应速度

#### 其他改进

- **改进封面日志输出**：在封面相关日志中添加播放器名称前缀（`[wesing]`、`[kugou]` 等），便于多播放器环境下的调试和问题定位

#### 兼容性提示

- **这是预发布版本**：3.x 接口语义和 2.x 相比有明显升级，下游客户端建议按 `type + player + data` 的统一结构解析。
- **单播放器订阅建议改用 Per-player 路径**：请直接订阅 `/wesing/...`、`/cloudmusicv3/...`、`/qqmusic/...`、`/kugou/...`。

#### 已知问题

- **`v3.0.0-beta-1` 无法自动升级到当前版本**：由于 beta-1 使用了旧的更新判定逻辑，已经安装 beta-1 的用户无法通过自动更新挣脱出来，需要手动替换到当前版本一次，后续版本再恢复自动更新。