### 3.0.0-beta.13

这是 3.0.0-beta.13 预发布版本，重点修复酷狗伴唱（半场）模式下的歌词获取问题，并改进了悬浮歌词页的副歌词显示行为。本版本累积 beta.12 的全部变更。

#### Bug 修复

- **修复酷狗开启伴唱（半场）后无法获取歌词**：开启伴唱时酷狗会切换到客户端混音的派生 hash，krcs 歌词库不识别该 hash 导致歌词丢失。现重构歌词查询逻辑为 3 级回退策略：
  1. 先按播放 hash 查询 krcs；
  2. hash 无结果时，通过 mobilecdn 歌曲目录用「歌手 歌名」反查规范 hash，再次查询 krcs；
  3. 最后做严格关键字回退（要求曲名精确一致、歌手匹配、时长 ±5s 内），并优先选择官方推荐词条。
- **同曲目切换伴唱模式时复用已加载歌词**：当检测到曲名 / 歌手未变但本轮歌词获取为空（典型为伴唱模式切换导致的 hash 变化），保留上一次歌词，避免歌词条突然清空。
- **修复悬浮歌词页副歌词默认显示问题**：副歌词（`sub_text`，如音译 / 翻译行）此前只要存在就会渲染，现改为需通过 URL 参数 `sub_text` 显式开启；配置编辑器内同步增加「副歌词」开关。

#### 其他改进

- **歌词查询日志更可追溯**：保留来源标记（官方推荐 / 第三方）与候选时长差，便于排查匹配错误。

#### 升级与使用提示

- **副歌词显示行为变更（破坏性）**：从本版本起，悬浮歌词页默认不再显示副歌词。如需恢复旧行为，请在 URL 中追加 `&sub_text` 或在配置编辑器中打开「副歌词」开关。
- **酷狗伴唱场景**：升级后建议在伴唱开 / 关切换、纯音乐、Live / 现场版等场景实际验证一次，如发现误匹配（例如同名翻唱被错误命中）请反馈日志。

#### 兼容性提示

- **这是预发布版本**：3.x 接口语义和 2.x 相比有明显升级，下游客户端建议按 `type + player + data` 的统一结构解析。
- **单播放器订阅建议改用 Per-player 路径**：请直接订阅 `/wesing/...`、`/cloudmusicv3/...`、`/qqmusic/...`、`/kugou/...`。

#### 已知问题

- **`v3.0.0-beta-1` 无法自动升级到当前版本**：由于 beta-1 使用了旧的更新判定逻辑，已经安装 beta-1 的用户无法通过自动更新挣脱出来，需要手动替换到当前版本一次，后续版本再恢复自动更新。
