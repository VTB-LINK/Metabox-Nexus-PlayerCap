### 3.3.1

在 3.3.0 基础上的增量：汽水音乐**逐行音译从无到有**（借酷狗歌词接口补全），跨平台音译对齐升级到**字级**、覆盖两边断行差异段；连带修复酷狗歌词搜索的一处关键词编码缺陷。**无破坏性变更，默认行为与 3.3.0 一致。**

#### 新增能力

- **汽水音乐逐行音译（roma_text）**：汽水平台自身无音译源，改由酷狗歌词接口按歌名 + 时长匹配「借」音译、按主歌词字级对齐重拼补入。**非平台原生、不保证稳定**：跨平台歌名搜索无法保证命中（可能搜不到对应曲，或匹配到不同版本 / 母带 / 翻唱），匹配失败或个别行对不上时该行留空，且绝不阻断汽水主歌词与翻译（能力表标 ⚠）。

#### 变更 / 改进

- **借取音译升级为字级对齐**：汽水借酷狗音译由行级文本匹配升级为逐字（rune 级 LCS）对齐，覆盖汽水与酷狗**断行不同（M:N）**的段落；小书写体假名（如 サィレンス ↔ サイレンス）视作同字救回，清浊音差异（並ふ vs 並ぶ）仍保持区分、宁缺毋错。
- **修复酷狗歌词搜索关键词编码**：歌名关键词的空格编码由 `+` 改为 `%20`（酷狗接口按字面处理 `+`、不作空格），修复多词关键词搜索退化、搜不到正主的问题。酷狗自身走 hash 直取不受影响，按关键词搜索的路径（含汽水借音译）受益。

#### 破坏性变更

- 无。

#### 已知限制

- Windows 专用。
- 汽水音译系跨平台「借取」，非平台原生，不保证每首命中或满覆盖。
