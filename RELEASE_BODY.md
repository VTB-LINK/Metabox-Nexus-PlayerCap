### 改进

- **播放器切换封面过渡**：`player_switch` 不再清除封面和旋转状态，保留旧封面由后续 `song_info_update` 自然触发交叉淡入淡出动效，与同播放器切歌体验一致。
- **移除 idle 状态重置**：`waiting_song`/`standby` 等非活跃状态不再清空封面、歌词和进度条，避免播放器切换瞬间（如 WeSing 关窗→网易云继续）出现封面停转和画面闪烁。