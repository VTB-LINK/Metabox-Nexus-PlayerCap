### 3.0.0-beta.10

这是 3.0.0-beta.10 预发布版本，主要新增酷狗音乐支持，并修正多个播放器的 `play_time` 输出。

### 重点更新

- **新增酷狗音乐支持**：正式接入酷狗音乐播放器（`kugou`），支持实时歌词、歌曲信息、封面、纯音乐检测（`index: -1`），与其他播放器行为对齐。
- **`play_time` 修正（CloudMusic / KuGou / QQMusic）**：修正了三个播放器错误地将歌词对齐 `offset` 叠加到 `play_time` 和 `progress` 输出的问题。`offset` 现在只影响歌词行匹配（提前选中下一行），`play_time` / `progress` 始终反映真实播放位置。Wesing 原本已是正确实现，不受影响。
- **封面分辨率提升**：Wesing 封面从 500px 提升至 800px；酷狗封面从 120px 提升至 800px。
- **修复：酷狗 VIP 歌曲首次播放获取不到封面**：重构封面获取逻辑，首次播放 VIP 曲目时也能正确推送封面。
- **酷狗进程退出后立即停止 CDP 轮询**：进程退出时不再等待 90s 超时，降低日志噪音。
- **酷狗 10.x 明确拒绝**：检测到版本前缀为 `10.` 时直接跳过 patch 并输出警告，避免在不支持版本上产生未定义行为。

### 兼容性提示

- **`play_time` 行为变更**：如果下游客户端此前依赖带 offset 偏移的 `play_time` 值，upgrade 后请重新验证时序逻辑。
- **`index: -1` 为通用纯音乐信号**：适用于 cloudmusicv3、qqmusic 和 kugou，后续新接入播放器亦遵循相同约定。
- **酷狗支持版本**：当前仅验证酷狗 v11.x 系列，10.x 明确不支持，其他版本将自动回退到最近的已知偏移并输出警告日志。
- **这是预发布版本**：3.x 接口语义和 2.x 相比有明显升级，下游客户端建议按 `type + player + data` 的统一结构解析。
- **单播放器订阅建议改用 Per-player 路径**：请直接订阅 `/wesing/...`、`/cloudmusicv3/...`、`/qqmusic/...`、`/kugou/...`。

### 已知问题

- **`v3.0.0-beta-1` 无法自动升级到当前版本**：由于 beta-1 使用了旧的更新判定逻辑，已经安装 beta-1 的用户无法通过自动更新挣脱出来，需要手动替换到当前版本一次，后续版本再恢复自动更新。