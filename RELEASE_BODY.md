### 3.2.0

在 3.1.1 基础上的功能性版本：引入运行期**中英双语（i18n）**，按系统语言自动切换界面与日志语言；新增自动更新下载的**任务栏进度**；**支持 QQ 音乐 22.60**；并补齐部分 CLI 参数。**无破坏性变更，默认行为与 3.1.1 一致。**

#### 新增能力

- **运行期中英双语（i18n）**：按系统语言在中 / 英之间自动切换，覆盖首启 Banner、更新提示框、隐私告示与采集告知、运营日志（log / err 两类），以及 overlay 页面（歌词页 / 特效页）。
- **自动更新下载进度同步任务栏**：下载新版本时，Windows 任务栏图标实时显示下载进度。
- **QQ 音乐 22.60 支持**：补齐 22.60 的内存偏移（歌名 / 歌手 / 专辑 / 进度 / 时长 / songID），修复升级到 22.60 后歌词与播放状态刮取失效的问题（#57）。字段布局延续 22.41 / 22.52 宽字符模型；未收录版本仍按最近已知版本回退。
- **CLI 参数补齐**：为「指定优先播放器」与「网易云特效最小化策略」补上命令行 flag，并补齐参数文档。

#### 变更

- **歌词 overlay 短行入场**：短行改为整行淡入，进入动画的提前入场上限由 430ms 收紧到 100ms。

#### 破坏性变更

- 无。

#### 已知限制

- Windows 专用。
- i18n 为中 / 英双语，按系统语言切换。
