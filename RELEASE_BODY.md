### 3.0.0-beta.8

这是 3.0.0-beta.8 预发布版本，主要新增 QQ 音乐纯音乐支持和 v21.81 兼容。

### 重点更新

- **QQ 音乐纯音乐支持（`index: -1`）**：QQ 音乐现在与网易云音乐保持一致——当确认当前曲目无歌词时，`lyric_update` 事件返回 `index: -1`（`text`/`sub_text` 均为空字符串，`timestamp`/`progress` 为 `0`），同时发送 `all_lyrics`（`count: 0`，`lyrics: []`）。客户端用 `index === -1` 识别纯音乐，无需依赖空 `text`。`index: -1` 语义现为所有播放器的通用约定，而不再是网易云专属。
- **QQ 音乐 v21.81 兼容**：新增 v21.81（32-bit x86）内存偏移表，SSO 字符串布局与 v22.16 相同，已借助 CE Lua 逆向验证。

### 兼容性提示

- **`index: -1` 为通用纯音乐信号**：原先文档标注为"仅 cloudmusicv3"，现已推广至所有支持纯音乐检测的播放器（当前为 cloudmusicv3 和 qqmusic），后续新播放器亦遵循相同约定。
- **这是预发布版本**：3.x 接口语义和 2.x 相比有明显升级，下游客户端建议按 `type + player + data` 的统一结构解析。
- **单播放器订阅建议改用 Per-player 路径**：如果只消费某一个播放器的数据，请直接订阅 `/wesing/...`、`/cloudmusicv3/...`、`/qqmusic/...`。
- **QQ 音乐版本**：已验证 v20.05、v21.81、v22.16 和 v22.22，其他中间版本暂无偏移数据，将自动回退到最近的已知版本偏移并输出警告日志。

### 已知问题

- **`v3.0.0-beta-1` 无法自动升级到当前版本**：由于 beta-1 使用了旧的更新判定逻辑，已经安装 beta-1 的用户无法通过自动更新挣脱出来，需要手动替换到当前版本一次，后续版本再恢复自动更新。