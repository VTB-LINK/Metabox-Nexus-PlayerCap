# 行尾规范：仓库内一律 LF，checkout 出来也一律 LF。
#
# 背景（别删这段，删了会有人把它改回去）：
# 本仓库所有 blob 本来就是 LF，但开发机普遍 core.autocrlf=true，checkout 时会转成 CRLF。
# 后果是 `gofmt -l` 把全部 42 个 .go 文件都误报成「格式不对」——因为 gofmt 只认 LF。
# 而其中真实的格式违规只有 5 个，假阳性率 37/42 = 88%，真违规就藏在这堆噪音里，
# 导致 gofmt 长期无法作为提交门禁使用。
# 显式声明 eol=lf 后，行尾不再取决于每个人的本地 core.autocrlf 配置，gofmt -l 才有意义。
#
# 上面这组数字是在隔离克隆里复现出来的（clone → core.autocrlf=true → checkout c38dd47^
# → gofmt -l 得 42，转 LF 后重测得 5）。旁证：这 5 个真违规恰好就是紧随其后的
# commit 813916d 所修的 5 个文件。
# 之所以专门注明取数方式：本注释初版写的是「22/42」和「92%」，两个数字都是错的，
# 而且互相矛盾（22/42=52%≠92%）——来源是当时的工作区已被工具部分改写成 LF，
# 我把那个脏读数当成了仓库事实。**在脏工作区里量出来的数，不是仓库的属性。**
* text=auto eol=lf

# 二进制：显式声明，不依赖 git 的启发式猜测。
# .syso 是 Windows 资源链接器输入，一旦被当文本做行尾转换会直接损坏，链出来的 exe 图标/版本信息全废。
*.syso binary
*.ico  binary
*.png  binary
*.exe  binary
